# Configuration

These utilities are intentionally light on configuration.

## Environment Variables

None required.

## Logging

- `utils.fs` uses a logger named `"workshop.utils.fs"`.
- Set the log level in your app:

```python
import logging
logging.basicConfig(level=logging.INFO)
```
